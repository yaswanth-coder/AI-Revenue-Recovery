# Server
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/recoverai

# Client
CLIENT_URL=http://localhost:5173

# AI (optional — system works fully without this via deterministic fallback)
AI_API_KEY=
AI_MODEL=gemini-pro
AI_PROVIDER=gemini

# Demo
DEMO_MODE=true
